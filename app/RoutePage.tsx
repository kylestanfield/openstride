import { Text, View } from "react-native";
import { RouteProp, useRoute } from "@react-navigation/native";

export default function RoutePage() {
  return (
    <View>
      <Text> This is the route page.</Text>
    </View>
  );
}
